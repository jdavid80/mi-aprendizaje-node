/* Para poder acceder al módulo debemos especificar la ruta */
/* Llamando al módulo */
const saludo = require("./6.1-mi-primer-modulo.js");

/* Llamamos al módulo(saludo),
llamamos a la propiedad y
le pasamos un argumento*/
console.log(saludo.saludarProp("David"));
